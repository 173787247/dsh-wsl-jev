# Changelog

## 0.1.0

- Initial release: `jev_status`, `jev_ask`, `jev_check`, `jev_rank`.
- OpenRouter System One (`OPENROUTER_API_KEY`) or TypeSafe (`TYPESAFE_API_KEY`).
- Outbound via `HTTPS_PROXY` / `HTTP_PROXY` (`https-proxy-agent`).
- No third-party Jev dsh/MCP plugin dependency.
