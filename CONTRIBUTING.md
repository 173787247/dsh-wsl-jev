# Contributing

Issues and PRs welcome. Keep the plugin outbound System One only — no chat-model fake Jev, no third-party Jev dsh/MCP wrapper dependency.

```sh
npm test
```

Env for live smoke: `OPENROUTER_API_KEY` or `TYPESAFE_API_KEY` (see `examples/dsh-wsl-jev.env.example`).
