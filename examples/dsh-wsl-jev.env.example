# Optional: source before restart-dsh-web.sh (or put in ~/.dsh/dsh-wsl-jev.env)

# Prefer OpenRouter (billed to that account):
# OPENROUTER_API_KEY=sk-or-...

# Or TypeSafe early-access key:
# TYPESAFE_API_KEY=...

# DSH_JEV_MODEL=jev-latest
# DSH_JEV_PROVIDER=auto
